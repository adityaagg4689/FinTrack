# render.yaml
services:
  # Backend API Service
  - type: web
    name: finance-tracker-api
    runtime: node
    repo: https://github.com/adityaagg4689/FinTrack
    branch: main
    plan: free
    buildCommand: cd server && npm install
    startCommand: cd server && node src/index.js
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_SERVICE_ROLE_KEY
        sync: false
      - key: SUPABASE_ANON_KEY
        sync: false
      - key: CORS_ORIGIN
        sync: false
      - key: JWT_SECRET
        generateValue: true

  # Frontend Static Site - Changed from "static" to "web" with static files
  - type: web
    name: finance-tracker-web
    runtime: static
    repo: https://github.com/adityaagg4689/FinTrack
    branch: main
    buildCommand: cd client && npm install && npm run build
    staticPublishPath: ./client/dist
    headers:
      - path: /*
        name: X-Frame-Options
        value: DENY
      - path: /*
        name: X-Content-Type-Options
        value: nosniff
      - path: /*
        name: X-XSS-Protection
        value: 1; mode=block
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        sync: false
